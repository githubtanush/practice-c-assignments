//console screen = mein aata output output top left mein print hoga

#include<stdio.h>
int main(){
    printf("Hello");//semicolon is necessary for end printf used to print hello on screen file saved in hello .something format show output then automatically close when we stop the console screen then we write return 0 or getchar to stop screen at that time.
    getchar();//getchar means its weight user to enter any character to end the screen.
}